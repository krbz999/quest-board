<section classes="{{#unless wide}} two-column" {{/unless}}>
  <div class="avatar">
    <figure data-action="showImage">
      <img src="{{avatar}}" alt="{{name}}">
      {{#if titles}}<figcaption>{{titles}}</figcaption>{{/if}}
    </figure>
  </div>
  <div class="statistics">
    {{#each stats}}
    {{#if show}}
    <p>
      <span class="label">{{localize label}}.</span> {{value}}
    </p>
    {{/if}}
    {{/each}}
  </div>
</section>
