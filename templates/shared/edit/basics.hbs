{{!--TODO: remove label once https://github.com/foundryvtt/foundryvtt/issues/12272 is resolved --}}
{{formGroup fields.name label=(localize "Name") value=source.name rootId=rootId placeholder=(localize "Name")}}
{{formGroup field label=label value=value rootId=@root.rootId choices=options}}
{{formGroup fields.category label=ctx.category.label value=source.category options=ctx.category.options rootId=@root.rootId}}
